<form method="post" action="{{ path('app_client_reserve', {'id': vehicule.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="button card-footer-item">Réserver</button>
</form>